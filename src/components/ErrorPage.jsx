import cl from '../styles/errorPage.module.scss';

const ErrorPage = () => {
  return (
    <div className={cl.wrapper}>
      <h2 className={cl.heading}>There has been an error...</h2>
    </div>
  )
}

export default ErrorPage
